# centralu-linux-arm64

The Linux (arm64) app bundle for [Centralu](https://github.com/ijun17/centralu),
shipped as an AppImage.

**Do not install this directly** — the `centralu` package pulls it in as an optional
dependency and picks the right bundle for your machine.

```bash
npm i -g centralu
```
